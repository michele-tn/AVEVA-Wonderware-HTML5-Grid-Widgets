Release GB_AGGridSQLWidget_mod2
Versione: v2026.09.10_1549
Creata: 2026-09-10 15:49:00 +02:00

Modifica RowsToSelect:
La riga viene evidenziata solo quando il valore ordine della riga coincide con il valore passato a RowsToSelect.
Sono riconosciuti i campi Ord, ORD, Ordine, OdP, ODP, Odp.
Rimosso il fallback che selezionava automaticamente l ultima riga con ordine non nullo.
Aggiunta una classe visuale dedicata alla riga selezionata da RowsToSelect, cosi lo sfondo resta evidenziato anche senza click del mouse.
Aggiunta applicazione diretta dello sfondo sulle celle della riga che matcha RowsToSelect, anche quando non sono presenti ConditionalCellStyles.
Aggiunta forzatura DOM post-redraw sulle celle della riga matchata da RowsToSelect, con background-color important.
Aggiunto match ordine su tutti i valori scalari della riga quando RowsToSelect usa un campo ordine, cosi funziona anche se il nome interno della colonna non coincide con OdP/Ordine.
Aggiunta estrazione RegEX del numero ordine da RowsToSelect. Esempio: da "Ordine=OPR26-30454||" viene estratto solo "OPR26-30454" e confrontato con la colonna OdP/ordine della riga.
Aggiunta property diagnostica RowsToSelectDebug per leggere da AVEVA raw, ordine estratto, righe analizzate, match trovati e indici matchati.
Correzione 2026-09-10:
La forzatura DOM di RowsToSelect ora ripulisce tutte le righe/celle visibili prima di applicare SelectedRowBackgroundColor solo agli indici correnti. Questo evita che righe non piu correnti restino evidenziate dopo il cambio di RowsToSelect o dopo un redraw AG Grid.
Correzione 2026-09-10 - evidenziazione esclusiva:
La classe standard AG Grid .ag-row-selected non usa piu SelectedRowBackgroundColor. Il colore giallo viene applicato solo alle righe che hanno match con RowsToSelect tramite gb-rows-to-select-match. In questo modo una riga cliccata manualmente non puo essere confusa con una riga ordine corrente.
Correzione 2026-09-10 - match stretto su ordine:
RowsToSelect ora confronta solo campi ordine/OdP espliciti e colonne riconosciute come ordine. Rimosso il fallback che confrontava tutti i valori scalari della riga, per evitare evidenziazioni di righe non correnti.
Correzione 2026-09-10 - isolamento visuale:
Quando RowsToSelect e attivo, tutte le righe non matchate vengono forzate al colore non evidenziato. Questo impedisce ai colori alternati odd/even o a una precedente selezione mouse di sembrare evidenziazioni da ordine corrente.

Regola AVEVA WONDERWARE:
Il file CWP importabile resta GB_AGGridSQLWidget_mod2.cwp.
Il nome interno del Widget resta GB_AGGridSQLWidget_mod2.

SHA256 GB_AGGridSQLWidget_mod2.cwp: B5150291BF66A3A21551D9DB7120DD6D58AE05BFCC97E6E9AAE8839D1F0FB338
